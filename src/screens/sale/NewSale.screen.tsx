import { StyleSheet, Text, View } from 'react-native'
import React, { memo } from 'react'

const NewSaleScreen = () => {
  return (
    <View>
      <Text>NewSale.screen</Text>
    </View>
  )
}

export default memo(NewSaleScreen)

const styles = StyleSheet.create({})