import { StyleSheet, Text, View } from 'react-native'
import React, { memo } from 'react'

const SelectItemScreen = () => {
  return (
    <View>
      <Text>SelectItem.screen</Text>
    </View>
  )
}

export default memo(SelectItemScreen)

const styles = StyleSheet.create({})